# Makorsha API base (no trailing slash required)
VITE_API_URL=http://localhost:8000/api/v1

# Cloudflare Turnstile site key (public)
VITE_TURNSTILE_SITE_KEY=

# Dev tools navbar button: shown only during `npm run dev` (import.meta.env.DEV).
# Production / Vercel builds omit it automatically — no env var needed.
