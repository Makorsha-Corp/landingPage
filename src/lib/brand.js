/** Product brand — single source of truth for landing copy. */
export const BRAND_NAME = 'Kolom';
export const BRAND_DOMAIN = 'kolom.app';
export const BRAND_PRODUCT = `${BRAND_NAME} ERP`;

export const supportEmail = () => `support@${BRAND_DOMAIN}`;
export const appOrigin = () => `https://app.${BRAND_DOMAIN}`;
