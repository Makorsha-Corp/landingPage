/** Primary tour building cutaway (95% quality export). Preload in index.html must match. */
export const PRIMARY_BUILDING_IMAGE = '/building-95.png'

/** Archives / alternate exports live in art-source/building/ (not served by Vite). */
