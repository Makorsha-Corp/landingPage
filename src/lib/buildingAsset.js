/** Primary tour building cutaway (full export). Preload in index.html must match. */
export const PRIMARY_BUILDING_IMAGE = '/building-full.png'

/** Pre-blurred hero overlay (95 export) — crossfades out on scroll; sharp layer hidden until then. */
export const BLURRED_BUILDING_IMAGE = '/building-95-blurred.png'

/** Archives / alternate exports live in art-source/building/ (not served by Vite). */
